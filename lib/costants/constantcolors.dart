import 'package:flutter/material.dart';

//these files are the constant colors used in whole app.
//we use constant colors here because we dont have to every file and change color if we want some change in future.
class ConstantColors {
  final Color lightColor = Colors.white;
  final Color darkColor = Colors.black;
  final Color primaryColor = Color(0xFFFFC93C);
  final Color primaryButtonColor = Color(0xFF2B2D38);
  final Color primaryFontColor = Color(0xFF5F4D1F);
  final Color darkFontColor = Color(0xFF3E4361);
  final Color purpleColor = Color(0xFF3E4361);
  final Color greenColor = Color(0xFF12A832);
  final Color blueColor = Color(0xFF2141E6);
  final Color redColor = Color(0xFFFF0000);
}